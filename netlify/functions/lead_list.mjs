import {db,json,errorResponse} from './shared.mjs';
export default async request=>{try{if(request.method!=='POST')return json({error:'POST required'},405);const {company_number}=await request.json();if(!company_number)return json({error:'company_number is required'},400);const c=await db().query('SELECT id FROM companies WHERE company_number=$1',[company_number]);if(!c.rowCount)return json({error:'Import the company profile before saving it'},404);let l=await db().query("SELECT id FROM lead_lists WHERE name='Default Strudel Leads' LIMIT 1");if(!l.rowCount)l=await db().query("INSERT INTO lead_lists(name,description) VALUES('Default Strudel Leads','Initial Strudel lead list') RETURNING id");await db().query('INSERT INTO lead_list_members(lead_list_id,company_id) VALUES($1,$2) ON CONFLICT DO NOTHING',[l.rows[0].id,c.rows[0].id]);return json({ok:true,lead_list_id:l.rows[0].id})}catch(e){return errorResponse(e)}};
